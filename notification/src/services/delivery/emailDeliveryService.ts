// import { getNotificationService } from '../integration';

// export class EmailDeliveryService {
//   async sendEmail(to: string, subject: string, body: string, provider: string) {
//     const emailService = getNotificationService('email', provider);
//     return emailService.sendEmail(to, subject, body);
//   }
// }
